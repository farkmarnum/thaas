org: farkmarnum
app: thaas
service: api

frameworkVersion: '3'

### Lambda + API
provider:
  name: aws
  runtime: nodejs14.x

functions:
  api:
    handler: handler.handler
    events:
      - httpApi: '*'

### DNS
plugins:
  - serverless-domain-manager

custom:
  customDomain:
    domainName: api.thaas.io
    basePath: api
    stage: prod
    createRoute53Record: true
