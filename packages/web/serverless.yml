org: farkmarnum
app: thaas
service: api

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x

functions:
  api:
    handler: backend/handler.handler
    events:
      - httpApi: '*'

plugins:
  - serverless-domain-manager

custom:
  customDomain:
    domainName: api.thaas.io
    basePath: ''
    createRoute53Record: true
    stage: ${self:provider.stage}

